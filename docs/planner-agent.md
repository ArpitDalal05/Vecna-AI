# Planning Agent

Decomposes user objectives into specific task assignments using structured JSON schemas.

* System Prompt location: `src/prompts/planner.md`.
* Fallback triggers: Invokes simulation planner if JSON parsing fails.
