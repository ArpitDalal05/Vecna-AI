# Filesystem Tool

Implements read/write actions on the local filesystem workspace.

* Sandbox bounds: Resolves relative path escapes (`..`).
* Actions: `readFile`, `writeFile`, `createFile`, `deleteFile`, `listDirectory`.
