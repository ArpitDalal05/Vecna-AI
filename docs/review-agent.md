# Code Reviewer Agent

Audits suggestions, computes metrics, and produces structural code reviews.

* Schema: Output matches: `{ score, recommendations, summary }`.
* Existing Review Engine hook: Wire recommendations into reviews list.
